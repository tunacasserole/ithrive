<div class="card">
  <div class="card-header m-b-20 bgm-bluegray">
    <h2>Manage indications <small>Indications are estimates only and subject to change upon completion of the full application.</small></h2>
    <a href="<%= new_quote_path %>">
        <button class="btn bgm-red btn-float waves-effect"><i class="zmdi zmdi-plus"></i></button>
    </a>
  </div>
  <div class="table-responsive">
    <table id="quotes-grid" class="table table-hover">
      <thead>
          <tr>
            <th data-column-id="agencyName">Id</th>
            <th data-column-id="agencyName">Name</th>
            <th data-column-id="agencyName">Premium</th>
            <th data-column-id="agencyName">Total Cost</th>
            <th data-column-id="agencyName">Status</th>
            <th data-column-id="agencyName"> </th>
          </tr>
      </thead>
      <tbody>
        <% @quotes.each do |quote| %>
          <tr>
            <td class="f-500 c-cyan"><%= link_to "#{quote.id}", edit_quote_path(quote) %></td>
            <td><%= quote.insured_name %></td>
            <td><%= quote.premium %></td>
            <td><%= quote.total_policy_cost %></td>
            <td><%= quote.state %></td>
            <td><%= link_to 'Delete', quote, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>

